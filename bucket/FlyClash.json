{
    "version": "0.2.6",
    "license": "",
    "extract_dir": "",
    "url": "https://github.com/GtxFury/FlyClash/releases/download/v0.2.6/FlyClash-0.2.6-x64-setup.7z#/dl.7z",
    "depends": [],
    "homepage": "https://github.com/GtxFury/FlyClash",
    "hash": "",
    "bin": "",
    "pre_install": "Remove-Item \"$dir\\`$*\", \"$dir\\uninst*\" -Force -Recurse",
    "installer": {
        "script": [
            "ensure \"$persist_dir\" | Out-Null",
            "$targetPath = \"$env:APPDATA\\flyclash\"",
            "$linkType = (Get-Item -Path $targetPath -ErrorAction SilentlyContinue).LinkType",
            "if (Test-Path $targetPath) {",
            "    if ($linkType -eq 'Junction') {",
            "        Remove-Item -Path $targetPath -Force",
            "    } else {",
            "        Get-ChildItem -Path $targetPath -Force | Move-Item -Destination $persist_dir -Force",
            "        Remove-Item -Path $targetPath -Force -Recurse",
            "    }",
            "}",
            "New-Item \"$env:APPDATA\\flyclash\" -ItemType Junction -Target \"$persist_dir\" | Out-Null"
        ]
    },
    "pre_uninstall": [
        "if (!(is_admin)) { error \"$app requires admin rights to $cmd\"; break }",
        "Start-Process \"$dir\\resources\\clash-verge-service-uninstall.exe\" -Wait -Verb 'RunAs' -WindowStyle 'Hidden'; Start-Sleep -Seconds 3"
    ],
    "uninstaller": {
        "script": "Remove-Item \"$env:APPDATA\\flyclash\" -Recurse -Force -ErrorAction 'SilentlyContinue'"
    },
    "shortcuts": [
        ["FlyClash.exe", "FlyClash"]
    ],
    "checkver": "github",
    "autoupdate": {
        "url": "https://github.com/GtxFury/FlyClash/releases/download/v$version/FlyClash-$version-x64-setup.7z#/dl.7z"
    }
}
